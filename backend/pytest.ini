[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    eval: LLM-judged evaluation tests (deepeval, requires Ollama + a local judge model). Opt-in only -- run via `pytest -m eval`.
addopts = -m "not eval"
